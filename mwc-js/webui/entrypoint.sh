#!/bin/bash
serve -s build -l 5000
#if [ "$NODE_ENV" = "development" ]; then
#  # Development environment: start the Node.js application using yarn start
#  yarn start
#else
#  # Production environment: serve the built application using serve
#  serve -s build -l 5000
#fi
